{
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "esbuild vendor.js --bundle --minify --format=esm --outfile=vendor.min.js",
    "pretest": "npm run build",
    "test": "node calculate.test.js"
  },
  "devDependencies": {
    "@ethereumjs/rlp": "5.0.2",
    "@noble/hashes": "1.4.0",
    "esbuild": "0.21.4"
  }
}
